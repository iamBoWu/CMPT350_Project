// import "./Category.css"
import React from 'react';
import {NavLink} from "react-router-dom";

// import {NavLink} from "react-router-dom";

const Order = ({items}) =>{


    return (
        <>
            {
                items.map(item => {
                    return (

                        <div>asdf</div>

                    )})
            }
        </>
    );
};


export default Order;
